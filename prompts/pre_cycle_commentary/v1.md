# Pre-Cycle Commentary — v1 user prompt template

Loaded by `lib/ai_pre_cycle_commentary.py` and substituted with `{bundle_text}` and `{run_date}` placeholders. SOUL.md is sent as the system prompt; this file is the user-message prompt.

Iteration policy: bump the filename (`v2.md`, `v3.md`) rather than editing in place. The wrapper reads `PROMPT_PATH = ROOT / "prompts" / "pre_cycle_commentary" / "v1.md"` — point it at the new version to switch.

---

## TEMPLATE START

# Pre-Cycle Commentary — {run_date}

Below is today's pre-cycle data bundle. Read it through the SOUL.md framework you've been given. The bundle has four parts:

  1. **Macro brief** — yield curve / FedWatch / Fed news (from Agent_Project ChromaDB).
  2. **Regime snapshot** — today's `regime_state` row plus 1-day-prior and 5-trading-day-average comparisons.
  3. **Verdict review (forward)** — today's `cycle_qualifier_runs` rows. These are the decisions in question. Focus interpretation here.
  4. **Context — Open positions (NOT under review)** — currently-open `placed=1` trades. Use ONLY for concentration / correlation math. Do not interpret as candidates for management — the mechanical layer (STP LMT at 2× credit, T-21 cue) governs those.

## Produce exactly these five sections

### 1. REGIME LANDSCAPE
One short paragraph. Current stage (0–4) + the most material 1d and 5d deltas. Whether macro convergence or divergence with the bull-put signal / H1 / IF gate. Cite via `[REGIME:]` and `[CURVE:]` / `[FEDWATCH:]` / `[FEDNEWS:]`.

### 2. VERDICT REVIEW
For each GO and each DOWNSIZE row in the verdict grid (skip PENDING / SKIP unless relevance is high):
- Annotate as **MACRO-ALIGNED**, **MACRO-NEUTRAL**, or **MACRO-COUNTER**.
- Cite the gate state that produces the verdict and the macro element that creates the alignment or tension.
- One short bullet per row.
- Format: `[QUALIFIER: ...]` for the verdict row + `[REGIME:]` / `[CURVE:]` / `[FEDWATCH:]` for the macro element.

If there are zero GO/DOWNSIZE rows, write `_No decision-relevant verdicts today._` and skip to §3.

### 3. CONCENTRATION & CORRELATION
Count sector load across open positions + today's GO candidates. Flag any case where adding a GO would push a sector to ≥ 3 (the cap is 2, per `[CONFIG: MAX_SECTOR_LOAD · 2]`). Flag any structure-clustering (e.g. all GOs are bull_put in stage 0 — fine; all are bear_call in stage 0 — surprising). Cite via `[POSITION:]` and `[QUALIFIER:]`.

### 4. CYCLE NARRATIVE
1–2 short paragraphs synthesizing §1–§3. No trade recommendations. No price predictions. Behavioral framing only — what to be alert to during execution, what would falsify a current expectation. Per SOUL §Behavioral Interpretation: the goal is to help the trader execute the existing framework consistently, not to add new opinions.

### 5. Closing
Exactly one of:
- `HYPOTHESIS [{run_date}]: ...` (only if the bundle contains a specific falsifiable claim)
- `NO HYPOTHESIS — today's data is consistent with the framework's prescription.`
- `NEXT QUESTION: ...` (a specific cross-reference the user should pull next session)

Per SOUL §Tone.

---

## Citation formats (extending SOUL §Quantitative Evidence Anchoring)

Three new formats for Phase 2. Apply the same discipline as the SOUL formats:

```
[QUALIFIER: opex · symbol · structure · verdict · reason-fragment]
  e.g. [QUALIFIER: 2026-07-17 · MSFT · bull_put · GO · contango+VRP+ gate ON]

[REGIME: snapshot_date · field=value (Δ vs Nd avg)]
  e.g. [REGIME: 2026-05-17 · spy_pct_to_ma200=+9.4% (-0.2pp vs 5d avg)]
       [REGIME: 2026-05-17 · stage=0 (no change vs prior close)]

[POSITION: id · symbol · structure · status]
  e.g. [POSITION: id=144 · JPM · bull_put · open]
```

(Macro formats `[CURVE:]` / `[FEDWATCH:]` / `[FEDNEWS:]` already defined in v1 of `lib/ai_macro_brief.py`.)

## Hard constraints (restated from SOUL §Mechanical Boundary)

- **Do not change verdicts.** GO/DOWNSIZE/PENDING/SKIP are from `cycle_qualifier.py`. Annotate; never override.
- **Do not generate new tickers.** Only names appearing in the bundle are in scope.
- **Do not predict prices** or the next move in SPY, VIX, individual names, or rates.
- **Do not recommend opens, closes, or rolls.** Position management is mechanical (STP LMT GTC at 2× credit, T-21 cue).
- **Cite every claim.** Paragraphs with no citation are opinion, not analysis — per SOUL §Lead with the number.

## Adequacy

This is a forward-looking commentary fired BEFORE the cycle plays out. There is no outcome data to validate against. Frame everything as expectation under the framework, not as confirmation that the framework is correct. Adequacy on any live-ledger reference still applies (🔴 PRELIMINARY / 🟠 SUGGESTIVE / 🟡 DEVELOPING / 🟢 ADEQUATE per SOUL §Adequacy Discipline).

---

{bundle_text}

## TEMPLATE END
